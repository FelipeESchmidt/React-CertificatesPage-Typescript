import { InfoProps } from '../AccordeonInfo/index.types';

export interface CertificateProps {
  id: string;
  fileSrc: string;
  courseUrl: string;
  imageSrc: string;
  imageAlt: string;
  info: InfoProps;
}
